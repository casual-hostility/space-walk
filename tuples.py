basic_classes = ('Warrior', 'Thief', 'Wizard', 'Healer')
print(basic_classes)

for i in basic_classes:
	print (i)

#basic_classes.append('Monk') #can't add to a tuple
#print(basic_classes)

basic_classes = ('Warrior', 'Thief', 'Wizard', 'Monk', 'Healer')
for i in basic_classes:
	print(i)

#tuples can be redefined but not edited
